你係 CCASS 系統 developer。有個緊急 bug：Run #30 同 #31 都行完 success，但 ccass.json 仲係 500 stocks（應該 2,777）。

## 你要做晒以下全部（唔好只 review，要 fix + test）

### 1. DEBUG: 點解 merge 完 ccass.json 唔 update？
- 讀 `ccass/src/runner.py` 全份，特別 `run_merge()` 同 `_export_json()`
- 讀 `.github/workflows/ccass.yml` merge job
- 讀 `ccass/src/db.py` 嘅 `_restore_db()` logic
- Root cause: 係咪 `_export_json()` 出 exception silent fail？係咪 DB path 錯？

### 2. FIX: 任何搵到嘅 bug
- Apply fix directly（用 `patch` write to files）
- 特別關注：`_export_json` 嘅 error handling，merge commit step

### 3. TEST LOCAL: 裝 deps + scrape 3 stocks verify pipeline
```bash
cd ccass_repo
pip install cloudscraper -r ccass/requirements.txt
cd ccass
python -c "
from src.db import init_db, get_conn
from src.scraper import CCASSScraper
from datetime import date

init_db()
with get_conn() as conn:
    codes = [r['stock_code'] for r in conn.execute('SELECT stock_code FROM stock_universe WHERE is_active=1 ORDER BY stock_code LIMIT 3').fetchall()]
scraper = CCASSScraper(delay_min=2, delay_max=5, timeout=30, max_retries=2)
for code in codes:
    snap = scraper.scrape_stock(code, date.today())
    print(f'{code}: {snap.num_participants if snap else \"FAILED\"} participants')
"
```

### 4. COMMIT + PUSH all fixes
```bash
git add -A && git commit -m "fix: ccass.json export + merge pipeline" && git pull --rebase origin main && git push origin main
```

### 5. TRIGGER workflow dispatch
Use Python:
```python
import keyring, urllib.request, json
token = keyring.get_password('git:https://github.com', 'rachellam28-lang')
# Get workflow ID, POST dispatch
```

## 環境
- Repo: /c/Users/Administrator/Desktop/automatic/codex_work/ccass_repo
- Universe: 2,777 stocks in ccass.db.gz
- ccass.db: 380KB (已 restore from gz, 0 daily rows)
- Windows, Python 3.11

## Output
做完每一步 report status。最後 confirm ccass.json 有新 data。
