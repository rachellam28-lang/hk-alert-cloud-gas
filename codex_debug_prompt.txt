你係 CCASS 系統 developer。有一個緊急 bug 要 debug：

## 問題
Run #30 CCASS Daily Scrape (6-Shard) completed SUCCESSFULLY（1h 51m, all 6 shards + merge passed），但 ccass.json 冇 update：
- ccass.json 仍然係 `updated: 2026-05-18, stocks: 500`
- GitHub commits page 冇 "chore: update ccass data" commit
- merge step 嘅 commit step 可能 silent pass（git diff --cached --quiet 回傳 true）

## 重要 context
- Universe 已 update 到 2,777 stocks（commit 855eb79, ccass.db.gz 入面 universe table 有 2,777 rows）
- ccass.db.gz 係 baseline DB（得 universe table, 0 daily/trends rows, 67KB）
- Workflow: 6 shards scrape → merge → trends → alerts → export → commit ccass.json + ccass.db.gz
- Run #30 用緊最新 main branch code（commit 98fcccc）

## 你要 check
1. 讀 ccass.yml workflow 嘅 merge job commit step — 有冇可能 `git diff --cached --quiet` 誤判？
2. 讀 `runner.py` 嘅 `run_merge()` 同 `_export_json()` — merge 完成後 ccass.json 有冇生成？
3. 讀 `runner.py` `_restore_db()` — restore from ccass.db.gz 後 DB 狀態正唔正常？
4. 讀 `scraper.py` — scraper 有冇 silent fail（HKEX 503 / token expired）？
5. 最重要：點解 6 個 shards SCRAPE 完（artifacts 有 upload）、merge 完（1m 40s）、但 ccass.json 冇新 data？

## Output
- Root cause analysis
- Concrete fix（patch format）
- 點樣防止將來 silent fail

Repo: /c/Users/Administrator/Desktop/automatic/codex_work/ccass_repo
Focus on the merge + export pipeline。唔好改 UI/frontend。
