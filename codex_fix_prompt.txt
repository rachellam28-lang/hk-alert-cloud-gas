APPLY ALL 3 FIXES from your diagnosis:

1. scraper.py — Stop catching RuntimeError (line ~203)
   - Change `except (RequestException, RuntimeError) as e:` → `except RequestException as e:`
   - This lets the "HKEX CCASS appears DOWN or BLOCKING" RuntimeError propagate up

2. runner.py — Catch RuntimeError in shard loop (line ~113-128)
   - Add `except RuntimeError as e:` handler that writes partial artifact and returns 2
   - Add internal deadline: `deadline = time.monotonic() + 6600` before the for loop, break if exceeded

3. ccass.yml — Run universe refresh once, not 6 times
   - Add a new `refresh-universe` job that runs once BEFORE scrape-shard
   - Upload universe DB as artifact
   - Remove `--refresh-universe` from all shard steps
   - OR simpler: just remove `--refresh-universe` from the shard command and add `max-parallel: 2`

Apply all fixes, then verify by reading each changed file back. Do NOT commit or push.